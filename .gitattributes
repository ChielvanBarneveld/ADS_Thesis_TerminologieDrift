# Normalise line endings: store LF, check out LF on every platform
* text=auto eol=lf

# Treat these as binary (no eol munging, no diff)
*.xlsx binary
*.parquet binary
*.docx binary
*.png  binary
*.pdf  binary
