# `code/SQ2/` — Regex drift sweep

Scripts for SQ2 (regex-based drift across the FORAS corpus + heatmap sweep).

> **Note** — this README is filled in as the corresponding `.py` files are added to the repo.

## Scripts (added incrementally)

| Script | Inputs | Outputs | Purpose |
| ------ | ------ | ------- | ------- |
| _(populated as we commit .py files)_ | | | |

## Run order

1. _(populated as we commit .py files)_
